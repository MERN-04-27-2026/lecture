// export default class Pizza {

// }

export const toppings = ["a", "p", "c"];
export const baseCost = { small: 1, medium: 2 };

class Pizza {}

export default Pizza;

// A module can only have one default export
// but as many named export as you like